let Util = require('../Util/Util');

class Media {

  constructor(name, type, url, title) {
    this.name = name;
    this.type = type;
    this.url = url;
    this.title = title;
  }

}

module.exports = Media;